@model EditBookViewModel;

@{
    ViewData["Title"] = "Edit book";
}

<form method="post" asp-controller="Home" asp-action="Edit">
    <div class="mb-3">
        <label for="title" class="form-label">Title</label>
        <input id="title" type="text" class="form-control" asp-for="Title" />
    </div>
    <div class="mb-3">
        <label for="author" class="form-label">Author</label>
        <input id="author" type="text" class="form-control" asp-for="Author" />
    </div>
    <div class="mb-3">
        <label for="genre" class="form-label">Genre</label>
        <select id="genre" class="form-select" asp-for="IdGenre">
            <option value="">Select a genre</option>
            @foreach (var genre in Model.Genres)
            {
                <option value=@genre.Id>@genre.Name</option>
            }
        </select>
    </div>
    <div class="mb-3">
        <label for="isAvailable" class="form-label">IsAvailable</label>
        <input class="form-check" type="checkbox" id="isAvailable" asp-for="IsAvailable" />
    </div>
    <div class="mb-3">
        <label for="image" class="form-label">URL Image</label>
        <input id="image" type="text" class="form-control" asp-for="URL_Image" />
    </div>
    <button class="btn btn-success" type="submit">Save</button>
    <a class="btn btn-danger" asp-controller="Home" asp-action="Details" asp-route-id=@Model.Id>Exit</a>
</form>
